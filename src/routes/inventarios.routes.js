import { Router } from "express";

const router = Router();

router.get("/", );
router.post("/", );
router.post("/productos/:idProduct/bodegas/:idBodega", saveProductExistBodega)


export { router };
