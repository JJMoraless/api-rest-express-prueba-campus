export const getMeteor = ({ params: { id } }, res) => {
  res.send(`meteoro ${id}`);
};

export const getMeteorsNear = (req, res) => {
  res.send(`meteoro ${id}`);
};

export const getMeteors = (req, res) => {
  res.send(`meteoro ${id}`);
};
